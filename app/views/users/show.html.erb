<h1>My Tools</h1>

<ul>
  <%= current_user.tools.each do |tool| %>
    <%= link_to tool.name, tool_path(tool) %> - <%= tool.quantity %>
  <% end %>
</ul>

<%= link_to "Add Tool", new_user_tool_path(current_user.id) %>
